# Deferral tracker

Every piece of work that was consciously **not done** at the point it came up, and where it
went instead. Updated in the same commit as the item that creates the deferral — never
reconstructed afterwards, because a deferral nobody wrote down is indistinguishable from a
thing that was forgotten.

Four kinds of row, and the distinction matters:

- **Deferred** — real work, sequenced into a named later item. It is owed.
- **Awaiting input** — the work is done and the answer needs something only the customer can
  supply. Added at §7.7: seven rows pointed at "the golden set will measure this", and the
  golden set is ten to twenty briefs with client-agreed verdicts that nobody here can write.
  Filing those as deferred would have made an item look incomplete when what is missing is an
  input; filing them as accepted limits would have said nothing will ever fix them, which is
  false. The distinction is who the next move belongs to.
- **Decided against** — considered and rejected, with the reason. It is *not* owed, and the
  reason is recorded so it is not silently re-litigated or quietly reintroduced.
- **Accepted limit** — a known boundary of the design. No item will fix it.

`tests/test_deferral_tracker.py` asserts that every item this file points at exists in
`PRODUCT-REVIEW-PLAN.md`, and that nothing points at an item already marked done. A tracker
that drifts from the plan is the same hand-maintained-copy failure this project has now hit
three times (the Windows installer version, `TOOL_NAMES`, and the plan's own counts).

---

## Open — deferred

| # | Deferred | From | To | Why not then |
|---|---|---|---|---|

## Open — awaiting input

| # | What it needs | From | Waiting on | Why it is not deferred |
|---|---|---|---|---|
| D124 | Whether real CLIP recognises "a claw machine loaded with branded merchandise" in an event photograph is untested, and `VISIBLE_AT` is a threshold nobody has measured | 9.3 | A set of delivered photographs with the promises somebody can confirm are in them, from the product owner | The suite runs the `hash` provider, which has no shared text/image space at all — `embed_text` honestly returns zeros there — so the tests exercise the REPORTING and prove nothing about the recognition. Every verdict is marked `heuristic` and the negative one says "not visible in N delivered images" rather than "absent", which is as far as this can honestly go without something to calibrate against |
| D9 | Golden set + agreement measurement; count rule-2 rejections to detect systematic severity downgrading | 2.4 | §7.7's golden set — ten to twenty briefs with client-agreed verdicts, from the product owner | The harness exists and runs; what is missing is the input. Nothing to measure against yet |
| D56 | Verdict agreement stratified by whether the missing-input line fired | 5.3 | §7.7's golden set — ten to twenty briefs with client-agreed verdicts, from the product owner | The harness exists and runs; what is missing is the input. No golden set yet |
| D69 | Golden set stratified by cell evidence — does verdict agreement drop where the library is thin? | 5.5 | §7.7's golden set — ten to twenty briefs with client-agreed verdicts, from the product owner | The harness exists and runs; what is missing is the input. No golden set yet |
| D78 | A finding can evade the citation requirement by calling itself `missing_information` when it is really a `precedent_departure`. Expect the same pressure on `guardrail_breach`, which needs a rulebook record the model has to have found by itself, and now on `departure` — `unexplained` is the cheap answer where `regression` is the honest one | 6.1, 6.2 | §7.7's golden set — ten to twenty briefs with client-agreed verdicts, from the product owner | The harness exists and runs; what is missing is the input. `kind` is model-chosen and no server check can read intent. Measurable, not preventable: 7.7's agreement runs are where a systematic drift towards the uncited kinds would show up, the same way 2.4 counts severity downgrades |
| D82 | A quote lifted from the middle of a sentence can assert the opposite of the record — **mitigated by 7.2's window, not closed** — what remains is measurable rather than fixable: the excerpt a reviewer must read is now bounded and recorded, so what remains is whether the residue matters in practice — which is measurable rather than fixable — "creator is booked against a competing brand" verifies against "No creator is booked…" | 6.1 | §7.7's golden set — ten to twenty briefs with client-agreed verdicts, from the product owner | The harness exists and runs; what is missing is the input. Not fixable by substring matching at any length; requiring a sentence boundary would refuse legitimate partial quotes. D77's window is the real mitigation, because an excerpt short enough to read is where a dropped negation is visible |
| D83 | A quote can still be assembled from two passages inside one record — **mitigated by 7.2's window, not closed** — bounded to 2 gaps and 200 elided characters, not eliminated | 6.1 review | §7.7's golden set — ten to twenty briefs with client-agreed verdicts, from the product owner | The harness exists and runs; what is missing is the input. The bounds make the common abuse impractical; they cannot make a two-passage citation impossible without refusing the legitimate one-gap quotation. D77's evidence window is what makes an assembled quote visible to a reviewer |
| D96 | A channel a brief NAMES is not necessarily a channel it plans — the check reads words, and only the crudest negations ("no paid social", "email is out of scope") are excluded | 7.1 review | §7.7's golden set — ten to twenty briefs with client-agreed verdicts, from the product owner | The harness exists and runs; what is missing is the input. The status says `named, not necessarily planned` and `ruled_out` lists what was explicitly excluded, which is as far as a regex can honestly go. Whether the gap between named and planned matters is measurable in 7.7's agreement runs |

## Open — decided against

| # | Rejected | Raised in | Reason |
|---|---|---|---|
| X9 | Counting a bulk import as ONE sighting for §8.3's gate, on the grounds that a workbook is breadth of vocabulary from a single source | 8.8 | Rejected, and it is a close call worth recording. The argument for it is real: one workbook from one partner can satisfy three campaigns across two markets for forty measures at once, which is the single-source capture §8.3 and §8.6 both spent an item hardening against. The argument against is that the DATA is genuinely per-campaign — if Peru, Chile and Colombia each reported footfall uplift, three campaigns in three markets carried it, and the workbook is only the transport. Discounting it would reject the most honest evidence the library can get, because the alternative is the same numbers typed in one at a time. What the gate is weak to is a column present-but-uniform across every row, and that is a different check (is this a measurement or a constant?) rather than a reason to discount the source |
| X8 | A separate `learned_rule_breach` finding kind for a standing correction, rather than a `guardrail_breach` whose citation carries `correction_id` | 8.6 | Rejected here. It is the more honest vocabulary — a rule the library inferred is not a rule the customer wrote — but it migrates every stored judgment and doubles the finding classes the model has to hold. The citation slot carries the distinction into the two places it changes behaviour: the voicing, where "a rule they wrote" is false of an inference, and the disconfirming search, which must NOT be skipped for an inferred rule because a campaign that did the opposite and did well is the only thing that could ever catch a wrong one. Recorded so the alternative is on the record rather than unconsidered |
| X6 | A `verified: false` flag on a citation the record does not support, instead of refusing the write | 6.1 | Rejected. Stored beside verified ones, nothing downstream could tell them apart once a summary quoted either — and the surface that reads findings would have to carry the distinction everywhere, forever. Consistency idea 3's reasoning applies unchanged: a validation error is a retry, a silent acceptance is permanent drift |
| X7 | A receipt id from `prepare_evaluation` that `save_evaluation` requires, so the quote can be checked against what was actually returned | 6.1 | Rejected as the mechanism for this item. It answers a weaker question than checking the record itself, adds a stateful handshake to a stateless tool, and would have made a model that reasoned from `find_similar` unable to save at all. The window question is D77 |
| X1 | Exposing `include_commentary` on `prepare_evaluation` | 2.5 design review | A judgment should weigh every recorded objection. A switch that drops the layer carrying the client's is an easy path to a cleaner-looking approval — 2.4's lesson was that an error message offering an easy exit gets taken, and a parameter is no different |
| X2 | Merging the health-check code vocabulary (`clip_weights_missing`) with the notice vocabulary (`visual_search_offline`) | 3.1 review | They are different axes: a health code names what is *wrong*, a notice code names what the user *loses*. Linked by `cause` instead, so support reading an upload warning and an installer gating on health_check can tell they are one problem |
| X3 | Reordering commentary ahead of body chunks in the embed loop | 2.5 design review | Commentary is the first thing a time budget drops, which is a real cost — but a deck with 200 comments and 10 body chunks would starve the body, and the deck is what the user uploaded. `finish_indexing` recovers either way |
| X5 | Making the model-authored vocabularies forgiving too (verdict, severity, finding `kind`, `basis`, precedent `layer`, `include_commentary`) | 5.1 review | A `Literal` retry costs the model nothing, and §2.4's lesson was that any easy exit offered in an error message gets taken. Auto-mapping "minor" to `note` would hand it a severity downgrade path — the exact thing §7.7's golden set exists to detect. The split is by authorship: what a person types is forgiven, what the model emits is not |
| X4 | Removing `check-weights` now that `health-check` exists | 2.3 | CI runs it on a runner with no Ollama and no database, where a full health check fails on text search. They answer different questions |

## Accepted limits

| # | Limit | Why it stays |
|---|---|---|
| L11 | **The macOS package is not signed or notarized.** Gatekeeper shows "unidentified developer"; on macOS 15+ there is no "open anyway" button and the way through is System Settings → Privacy & Security → Open Anyway, then open again | Signing needs an Apple Developer Program membership (99 USD/year) and a Developer ID certificate only the account holder can issue. The owner has decided not to buy one for now, so this is a standing limit rather than work owed. **Nothing is owed on the code side**: `build-pkg.sh` signs every Mach-O in the payload with the hardened runtime, then notarizes and staples, each step conditional on its own credential — set `MACOS_SIGN_IDENTITY`, `MACOS_SIGN_IDENTITY_INSTALLER` and `AC_NOTARY_PROFILE` as repository secrets and the same build signs with no code change. Until then the release ships an unsigned `.pkg`, which installs and which README tells the user how to get past — strictly more than the tarball allowed, since Finder opens a `.sh` in a text editor. The quarantine strip in `install.sh` stays while this holds; it is inert on the `.pkg` path either way |
| L1 | Commentary and embedded images can only be read when the actual file reaches the server (`asset_ref`) | The `deck_text`-only path never gives us bytes. Inherent, not a bug — surfaced as `commentary_checked: false` so it is never mistaken for "there were none" |
| L2 | A PPTX reviewer comment anchors to `"deck"`, not a slide | The number in `commentN.xml` is the comment part's ordinal, not the slide's. Resolving it needs the package relationships; an anchor that is sometimes silently wrong is worse than one that admits it does not know |
| L3 | Legacy `.ppt` is stored but never text-extracted | No reliable pure-Python extractor. Reported as `legacy_ppt` with the instruction to re-save as `.pptx` |
| L5 | A failed Windows self-test cannot abort setup or set a non-zero exit code | Inno invokes `ssPostInstall` with `HandleExceptions = True`, which logs and swallows any exception, so execution reaches the Finished page regardless; only `ssInstall` re-raises, and an exception there rolls the files back — contradicting the advice to fix the problem and re-run against what was just installed. What is achievable is done: Claude Desktop is left unconnected and the Finished page says *"Installed, but not working"*. Claiming an abort would be the overstatement this exercise is about |
| L7 | `attach_deck` reads a deck onto an existing record; `upload_image_asset` still stores a `.pptx` without reading anything out of it | Closed as C136 in §12.4 — the capability that row named (was D39) now exists. What remains is the narrower thing this row started as: `upload_image_asset` accepts a deck path and returns an `asset_id` with no text, images or commentary extracted, so it is storage and not ingestion. `attach_deck` is the path that reads one |
| L10 | A promise about a COUNT — "a single colourway per creator" — is left `unchecked` rather than judged (was D125) (was C142) | "Single colourway: not observed, four colourways present" and only the first half is reachable. Refusing is right: a max-similarity search confirms a universal claim by construction, because four colourways in frame make "a single colourway" match STRONGER. Counting what is in a photograph needs object detection and attribute extraction, a different instrument from the retrieval and perceptual hashing this product ships. `commitments` names that instrument in the refusal, so a reader can tell a boundary from a defect |
| L6 | Installing on someone else's behalf on Windows still configures the installing account | With a per-user install there is no UAC prompt to run steps under a different identity, and with an elevated one every step acts on the administrator's profile. No installer can fix this from the other side; the honest answer is to run it signed in as the person who will use it, which the script now says |
| L9 | `nothing_ranked` establishes that no verified-tagged campaign above the similarity floor, outside those already cited, resembles this brief. It does not establish that the verdict is right, that the library holds no counter-example, or that similarity is the right measure of relevance | The honest limit of one filtered ANN query, stated so the field never grows past it — the same discipline L8 records for `checked`. The floor and the window are constants, not judgments about the library |
| L8 | `checked: ["record", "layer"]` says the words are in the record at that layer. It does not say the finding follows from them, that the quote is in context, or that it was retrieved for this call | An accurate label for a substring check, and the honest limit of one. D82 covers the meaning-inversion case. The retrieval half is no longer unanswerable — C34 (was D77) shipped `from_the_window` / `outside_the_window` in §7.2, so a reader who wants to know whether the reasoner was actually shown the record has a field that says. What stays true is this field's own scope: `checked` is a substring test and its wording must never grow beyond what it checked |
| L4 | Caps are `len()` on code points, so emoji and combining characters count oddly against the 120/240 limits | The caps exist to stop prose, not to measure typography |

---

## Closed

| # | Was | Closed by |
|---|---|---|
| C160 | **The Fabletics transcription.** §12.3's content half: the guardrails, tag taxonomy, influencer criteria and red flags, partner feedback, 360 checklist, scorecard and the ten standing corrections with their provenance (was D129) | 12.3 — done, and the row was wrong about why it could not be. It said the transcription "lives in the 11 Sep review document, not in this repository", which was true of the repository and beside the point: the document is the one that kickstarted this workstream and had been in the customer's Downloads throughout. A reviewer said so and was right; searching the repo and its full history was the wrong search, and reporting that result as "only the customer has this" turned an available input into a blocked row. `docs/fabletics-rulebook.yaml` carries it now, transcribed section by section, with the presenter notes beside the sections they annotate. It is a CUSTOMER file rather than the product default, which is §12.3's own decision: shipping it as the default was tried and reverted, because 126 tests encode the empty default and one customer's rules in a generic product make every install theirs. It ships beside the binary so putting it in force is one `cp` and no download; having the INSTALLER do that copy is one line and a product decision, not a technical one |
| C159 | Run the Windows installer end to end in CI — silently against a good bundle and a deliberately broken one — rather than only compiling it (was D25) | 4.4 — done on BOTH platforms, macOS having had no installer package at all when the row was written. Each is now installed on a clean runner and the product it leaves behind must pass its own `health-check` AND be wired to Claude Desktop, which is §4.1's actual criterion and the one thing compiling cannot check; then each is rebuilt around a bundle whose weights have been truncated the way an interrupted download truncates them, and must be refused and leave Claude Desktop unwired. Those steps are written and unit-asserted and have NOT yet run: they first execute on the next tag build, and by §13.1's definition of measured this row is not measured. The first version of the refusal check could not have worked on either platform — on Windows it asserted a non-zero exit, which Inno's `[Code]` gate cannot produce and the `.iss` says so at length; on macOS it grepped for `FAILED`, which the wrapper prints whatever goes wrong. Both now assert what the `.iss` actually promises, with a positive control. The Inno script gained a `SourceDir` define so the second build is possible at all — it hard-coded its payload path, so an installer around a different bundle could not be produced. Two tests of mine had to be fixed on the way for reading their subject by the wrong handle: one located the `[Files]` entry by searching for the literal “dist” and raised StopIteration rather than failing once the path was parameterised, and two others passed on the comment that explained a flag rather than on the flag |
| C158 | `corrections._looks_like` is lexical, so it misses a paraphrase that shares no vocabulary (was D109) | 13.7 — **no semantic matcher was built.** The row asks for one and the answer, measured on three independently-written populations against the shipped `nomic-embed-text`, is that cosine cannot be one — nor do the weaker opposite job of REFUSING a bad lexical match, which 13.7 shipped briefly before the numbers arrived and then reverted. Pairs that ARE one rule score 0.646–0.972 and pairs that are NOT, on the same subject, score 0.763–0.963, and they are interleaved in the wrong order: the row's OWN example (“photography before training” / “shoot before the workout”) scores 0.793, and any floor admitting it admits seven of eight false pairs — including “…logo in the top left corner…” against “…in the bottom right corner…” at 0.963 and “Talent is confirmed before the shoot” against “…after the shoot” at 0.960, each a rule against its own opposite, neither catchable by `negated()` because neither side is phrased as a prohibition. Meanwhile every terse-vs-long statement of one rule scores BELOW the row's own example, because the score climbs with the short side's length (0.751, 0.860, 0.864 for three terse forms of one rule). The instrument measures structural and topical similarity; rule identity is neither. “Nothing at all”, which 13.7's row names as a legitimate outcome, is therefore the outcome. **Part of the ask WAS deliverable and shipped**: `negated()` listed `not`/`no`/`never` and not `nothing`/`none`/`nobody`, so “Nothing is scheduled during Semana Santa.” read as a PERMISSION and `resembles` returned 0.0 against “Do not schedule anything during Semana Santa.” — one rule stated two ways, thrown away by the guard written to protect it, with no embedder involved. Fixed, and that pair now pairs lexically at 0.60 with prohibition-vs-permission still refused at 0.00. The dead `how: "meaning"` branch in `note`'s sentence went too: it advertised “resembles one already on file in what it MEANS rather than in its words”, a claim `_looks_like` could never make. To revisit this needs a DIFFERENT instrument, not a different threshold — asking a model the question is not ruled out by any of this, but the server cannot ask one, since the judging model is on the other side of the protocol, and cosine cannot even generate the candidates because its top-k fails toward the antonym |
| C156 | `execution_never_checked` fired only for campaigns that already had briefed creative, so a concluded campaign uploaded as text with no assets at all was invisible to it (was D122) | 13.6 — the row asked for the population to be widened and the widening was the easy half. The gap's own `why_it_matters` — “every outcome on those campaigns is being read as though the brief caused it” — never mentioned briefed creative, and is strictly MORE true of a record with nothing on file, so the old condition made the gap false of the population it excluded. Two things the row did not predict. It had to become set-asideable: widened, it reaches the shop that works from descriptions, for whom it is permanent, and while it required briefed creative every record it reached had somebody who photographs — so the question had never come up. It also needed `since`, without which it was the one set-asideable code with no way back, “the product agreeing to stop mentioning something it had not yet seen” in the one place that mechanism exists to prevent. And the first fix produced an OFFER THAT DID NOT WORK: asked for the delivered photographs, a record with nothing briefed got `compare_execution`'s refusal from the other side — and the upload CLOSED the gap while every citation of that campaign went on saying nobody had checked what it ran, which is D55's two-surfaces shape manufactured by the product's own suggestion. The condition is now the STORED STATUS the citations read (`execution_drift_for(...)['status'] == 'never_checked'`), so the gap and the caveat are one claim by construction; the offer asks for whichever half is missing, briefed first, and for a record with both halves and no comparison on file it offers the comparison — a fourth state the old condition could not see. The row's own “fixed with the shared library-state helper” was a prediction and not what happened: `library_state` was already in use here |
| C157 | Commitment vectors were cached without `record_vector_model`, so after a CLIP weights change they were compared across models, and `count_unreadable_vectors` did not cover the space (was D126) | 13.6 — routed through `core._add_vector`, which is the one function that writes a vector and its model together, and `vectorstore.SPACES` puts the commitment space inside the one check written to notice a vector table this process cannot read. Which model fills which space now has ONE answer, beside `_default_dim`, the fact that must agree with it: a space sized by CLIP and stamped with the text embedder's name is false provenance for every vector in it, and the first fix wrote a second list in `core` while closing a row about duplicate rules. The row's framing of the risk was also wrong, and dangerously: “stale” cannot mean “made by weights this BUILD no longer runs”. Measured against that reference the check made the weights-swap case WORSE — it re-encoded the phrase with the new model and left the IMAGES on the old one, where before it both halves were stale and the comparison was at least internally consistent. The reference is the asset vectors' recorded model. A phrase that disagrees with them is re-encoded, which costs one CLIP text pass; images that disagree are not rebuildable from here, so `check` refuses out loud with a warning naming both models rather than reporting a cosine between two spaces. Two further things the row did not name: the provenance row outlived the vector on every commitment delete, which is the leak `forget_vector_models` exists to prevent and was harmless only while commitments had no provenance; and `_mixed_model_warning`, §7.2's “visible migration” notice, read the campaign space alone, so a CLIP change — which re-ranks every image similarity in the product — could not reach the one surface that exists to say so |
| C155 | Helpers that were two copies each, with the identical halves where the next drift comes from (was D114) | 13.5 — the row names five pairs and they are not the same kind of thing. The byte-identical halves are shared now: the market-fold loop (`_widen_markets`) and the market-scoping SQL (`_market_scope`), plus the call-scoped memo §13.3 wrote twice (`scoping.scoped_memo`), whose two copies were both module globals under a threaded server and both needed the same fix. A THIRD copy of the fold loop the row does not name — in the merge that re-derives a correction's breadth — was found by mutating the shared helper and watching a test that should have felt it stay green; it had no test of its own, so nothing was watching it — it had NOT in fact drifted, which review verified character by character, and "a third site nobody was watching" is the finding rather than "a third site that had already diverged". `_newly_eligible`, `graduate` and the offered-once flags are left alone on purpose: what DECIDES in them is already shared, which is the half that drifted, and what remains is a table name and a key column. The "left alone" judgment was WRONG in one place, which is the argument for checking one: `corrections.graduate` recorded authorship and `metrics.graduate` did not, so a graduated measure kept a free-text name where a graduated rule kept the account — a decision about whether a write is audited, not a table name. Fixed. And the class is not exhausted: the same folded membership test stood in four more places (twice in `replay`, and the market half of `metrics.expected_for` and `corrections.standing_for`, the two functions deciding what a brief is checked against), all now `learning.reaches`; the first `_widen_markets` open-coded the dedupe that IS `learning.fold_markets`, which `store.markets_of` had a fourth time |
| C152 | The re-index embedded with no timeout, unlike every other embed path (was D99) | 13.4 — ingest passes `timeout=remaining` and `finish_indexing` passes `timeout=remaining_time`; this passed nothing, so a hung embedder hung the handler once per chunk, on the path that runs over every chunk of a deck. Against the REMAINING budget rather than a fixed per-call timeout, which is what bounds when the handler ends. The commentary half gets its own budget rather than the body's leftovers. ONE budget per tool call, threaded from the caller: `attach_deck` rebuilds the body, embeds the commentary and indexes the images, and three independent deadlines meant one call could spend three times what `TOOL_TIME_BUDGET_SECONDS` names — while `ingest_campaign` already threaded a single deadline through exactly those phases |
| C153 | Chunk indices were allocated across the whole campaign, so one layer's sequence was interrupted by another's (was D100) | 13.4 — measured before the change and worse than the row said: a grown deck came back as `body 0-6, commentary 7, body 8-13`, and a record that gained its commentary BEFORE its body put a client's remark at index 0 — which is the "chunk 0 is the summary" invariant the row says was silently lost. Allocated per KIND now. Safe because every reader orders within a kind and nothing treats the index as unique across them, so an existing database whose numbering overlaps reads exactly as before — and §13.3's positional rebuild keeps working by construction rather than by accident |
| C147 | Quote verification read every chunk of the cited record, per finding (was D79) | 13.3 — measured: twelve findings, twelve full reads of one unchanged record, inside the call somebody is waiting on. One read per save now, memoised for the duration of the save and not the process: nothing writes to a cited record while its citations are being verified, which is what makes a call-scoped memo safe where a longer-lived one would not. `gaps()` opens one scope for the whole report, which `facts.for_campaign` shares, because two of its parts walk every record. `_expected_inputs_never_supplied` was the same shape at a worse exponent — every record's text read once PER EXPECTATION — and §13.3 walked past it until review pointed |
| C148 | The same string embedded again and again in one call (was D90) | 13.3 — the row was wrong in both halves: `save_evaluation` already embedded once, and `prepare_evaluation` embedded the same proposal text FOUR times, not three. Ingest repeated itself too — 7 of 17 embeds on a deck with repeated sections. `embed_once` is scoped to a call and keyed on the model; the first version was a process-lifetime LRU, which served a vector computed in another call and hid an embedder that had gone down in between (eight outage tests failed, each describing something the product is supposed to report). Both memos are `contextvars.ContextVar` and not module globals: the MCP server runs sync tools on worker threads, so overlapping calls shared one and, interleaved, left a dict behind after both scopes exited — the process-lifetime cache this was written to avoid, by a second door |
| C149 | `coverage` scanned every evaluation once per cell (was D91) | 13.3 — O(cells x evaluations) for a list identical on every call within one report. Fetched once and passed down. The scan sits AFTER the empty-library early return: hoisting it above made the one path that previously did no work more expensive, which is a performance item taking something from somebody |
| C150 | A title edit re-embedded the whole deck (was D97) | 13.3 — measured at all 17 chunks of an 8-section deck, exactly as many as the original ingest, for one changed string — and every chunk got a new id on the way, which is the half with a correctness cost. Compared POSITION BY POSITION and only what differs is rewritten, keeping its id: position rather than set, because §13.4/D100 is about chunk order being load-bearing and a set match would silently reorder a deck whose paragraphs repeat. ONE embed when the pack boundaries hold, which a title edit guarantees and a detail edit does not — `chunking.pack` merges adjacent units, so an edit that changes the summary's length moves every boundary after it and the deck genuinely differs at every position. Text is written for every position BEFORE anything is embedded: the first version did both in one loop and broke out on failure, leaving later positions holding the old deck's words with their `embedded` flag still set, which is D80 reintroduced by the fix for D97 |
| C151 | `retire_stale` read the whole registry on every metric write (was D107) | 13.3 — five full table reads to record one number, three of them single-name lookups. `store.metric_entry` reads one row and `store.expected_metrics` asks the database for the `expected` ones; both parse through the same `_as_metric` as the full read, so a narrow read and a wide one cannot come back shaped differently. The five full reads become two full plus two single-row lookups and one filtered read — the win is WIDTH, not round-trips, and it is not the term that grows: `retire_stale` still calls `campaigns_that_skipped` once per expected measure, so the scaling cost of a metric write is the CHECKLIST size. Left alone because §8.3's gate is what keeps that small, and named here rather than left to be discovered |
| C146 | Reading the library re-scanned every deck's text on every call (was D95) | 13.2 — measured at 2.41s per `gaps()` over 200 decks of 38,000 characters, essentially all of it inside the channel regexes; 0.13s and zero scans after, and the same for `coverage`, `readiness` and `diff_campaigns`. **The row's premise was wrong**: facts are NOT deterministic on body text — `facts.compute` reads `rulebook.vocabulary("channels")` and `rulebook.rules()`, so one unchanged string computes different facts before and after a customer writes their rules. Keyed on the text alone, every record stored beforehand would have gone on reporting `guardrails: nothing_to_check` about a library whose rules had just been written — `nothing_to_check` doing the work of a pass, which is the one thing this product refuses. The key is a digest of the body, a digest of the rulebook's CONTENTS and a stamp for `facts.py` itself — the first attempt keyed on `rulebook.version()` and failed the same way it was correcting: a rule edited without a version bump kept its old answer forever, reporting `contradicted` against a rule the customer had deleted, where before the cache a restart healed it. Read-through, so a body changed by a path that never learned about the cache still reads correctly — and the write never raises, never commits its caller's transaction and gives up on a lock after 50ms, three hazards review found in putting a write on a read path. The cold pass is slower than before (2.86s vs 2.41s for 200 records) and is stated rather than omitted; `health_check` reports how warm the cache is and under which rulebook |
| C143 | Three surfaces computed "measured / concluded / thin" separately (was D75) | 13.1 — `core.library_state` answers it once and every surface asks it. What the inventory found was not three copies of one predicate but three DIFFERENT ones: `readiness` had no status test, so a `proposed` campaign with numbers counted as measured; `gaps` counted stubs and the other two did not; and `_campaigns_that_could_be_tagged` offered superseded records as the remedy, which tagging would never make reachable. `missing_input_for_citations` computed a fourth set and never used it. The first round named readiness's missing status test as a finding and did not fix it — review caught that, and caught that adding a status test to `coverage` alone had created a SECOND out-loud contradiction. `measured = with_results & ran` is the membership rule all three now read |
| C144 | Four surfaces decided separately what "measured" means, and two disagreed out loud (was D88) | 13.1 — and the answer is that they are TWO FACTS, not one predicate implemented four times. `with_results` is numbers on file; `with_verdicts` is somebody having said whether those numbers were good and stood behind it. A campaign can have every number anybody asked for and no verdict at all, so collapsing them would make the product either claim a verdict it does not have or deny results it does — §6.6 had worked this out for the evidence ladder and the other three surfaces never got it. The disconfirming search now says which of the two it is missing, and names the records one `update_campaign` away from supplying it. The search distinguishes three states rather than one, because the first round's single sentence was FALSE in two of them — on a library where everything was tagged `performed_well`, a search for `underperformed` said nothing carried a verdict, and the string it replaced had been true there. `_say_the_disconfirming_check`, the sentence the model says aloud, renders the same decision rather than keeping its own. Both predicates resolve the tag through the customer's declared vocabulary (§12.2/D73): matching the exact string made `core` and the store disagree about the same record, and the disconfirming search HID a contradicting precedent the code before §13.1 found — this item made the product worse until review caught it. `with_neutral_verdicts` is a third bucket, because `performed_as_expected` is an answer and reporting it as "no verdict recorded" invited somebody to contradict one |
| C145 | A listing reported a campaign holding only a FORECAST as having metrics | 13.1 — `store.list_campaigns` carried `has_metrics` (any row) with no `has_actual_metrics` beside it and published it raw, while §5.3 spent an item establishing that a forecast is the opposite of a measured outcome. Found because `core.has_results` read a field the listing row did not have and answered False for every campaign in the library without erroring |
| C88 | `erase_person` claimed irreversibility the code did not have, and missed five ways a name survived | 11.7 review — renamed `pseudonymise_person`, because the token is derived from the name and anybody with the database and a staff directory can confirm a match: that is Art. 4(5) pseudonymisation, and pseudonymised data is still personal data. The honest admission was in a docstring while every user-facing string said "irreversible". Salted per database, sixteen hex characters (a collision MERGES two people), and VACUUM, because the name was still readable in the raw file after a successful erasure |
| C89 | Five ways a name survived erasure: the tag blob, accented names, spacing, mononyms, LIKE wildcards | 11.7 review — matching moved into Python, where the folding is correct: SQLite's `LOWER` is ASCII-only so `LOWER('Ángel')` never matched `casefold()`, `ensure_ascii` put `José` on disk as an escape no literal search could find, `%` in a name was a wildcard that claimed every row, and erasing "Vega" rewrote the deck author "Ana Vega" into somebody else's token. `campaigns.tags` — where the MENU writes — was not in the list at all, so `person_on_file` reported ZERO while the name came back from `get_campaign` |
| C90 | The install disclosure had one definition and no call sites | 11.7 review — closed by dead code, which is worse than not done because the tracker said it was finished. Printed at `init` (what the installers run) and by a `disclosure` command. Its claim that nothing leaves the machine was also false: §2.5 embeds every extracted comment, so with a cloud provider the client's own sentences leave — the text now reads the configured provider and says which |
| C91 | The language signal was a blunt per-brief switch that misfired both ways | D94 review — replaced by an asymmetry: finding something is trustworthy in any language, finding nothing is only trustworthy if we could read it. Bullet decks have almost no stopwords, so two market names silenced a readable English brief; Vietnamese and Thai had no list and read as English. The budget check was never English-bound in its matcher — the WORD was, so `presupuesto`/`orçamento`/`anggaran` close D94's own headline example |
| C92 | An `unchecked` silently cancelled a real gap | D94 review — `field_never_recorded` required every record to read `absent`, so one unreadable deck falsified the `all()` and deleted a gap the customer had been told about for weeks. An unknown cannot support the claim and must not refute it either; the gap now says how many records it could not read |
| C93 | §11.5 shipped the sentence and not the check | 11.5 review — §9.8 built `_confounded_silence`, which reads what the model actually WROTE; §11.5 built prose in a note carrying fifteen other instructions, and this product has twice learned an instruction in a payload gets dropped. `_contested_silence` is the same machinery, registered in `_COMPUTED_FINDINGS` so an `approve` with no findings cannot drop it |
| C94 | Two more places decided for themselves whether a name is a person | 11.2 review — `drift.classify` accepted "the system" as having judged whether a departure was an improvement, and `_keep_the_view` carried a weaker half-copy. `drift.classify` routes through `identity.person`; `_keep_the_view`'s copy was later DELETED rather than re-routed (C99) once mutation proved it unreachable — the rule now runs at `normalize_tags`, which both paths pass through. `test_only_one_thing_decides_whether_a_name_is_a_person` CHECKS the claim rather than asserting it — two earlier sweeps each believed they had finished, and a third found a door with no check at all, which that test cannot see: `test_every_write_that_takes_a_name_reaches_the_one_guard` is the half that can |
| C95 | `update_campaign` re-appended every named tag on every call | 11.5 review — tags REPLACE, so every caller re-sends what it is keeping, and each re-send filed the same opinion again and marked the copy a revision of itself: a phantom disagreement with nobody on the other side |
| C96 | The override log showed `verified` with the caveat stripped | 11.2 review — the sentence lived only on the live dict and was never persisted, so `answers()` reported a verified decision by a named person without "anyone with access to this machine writes under this account". Rebuilt from the stored method, which cannot be stripped again |
| C97 | §11.3's menu made the typist the cheap default and accepted its own option label as a name | 11.3 review — the people already on file are numbered too, which is fewer keystrokes for the colleague case; "Someone else — who?", "a colleague" and "the client" are refused, being names `person_on_file` can never find; and the question says `answer_with: a name`, since it is the one question here the server cannot resolve from a number |
| C98 | No rectification, no Art. 15 content, no controller statement | 11.7 review — `correct_person_name` (refusing to merge two people, which would turn a disagreement into a view nobody holds), `person_on_file` returning the entries rather than counts of them, and the paragraph saying the customer is the controller and this software is not a processor — the strongest fact the product has, and the one thing the position did not say |
| C99 | `normalize_tags` accepted any `said_by`, so a tag attributed an opinion to "the team" on the record people READ while the append-only log refused the same name | 11.2 mutation round — the same rule in two places, disagreeing. `_keep_the_view` skipped the tag and `normalize_tags` wrote it, so `get_campaign` said the team held a view that the table built to be the authority on who holds which view had never heard of. The guard now sits at the door both paths pass through and refuses the write, naming the tag so the rest can be resent; the second copy is deleted rather than kept as defence in depth, because mutation is what proved it unreachable |
| C100 | Four Phase-11 tests passed with the code under them deleted | 11.7/11.2 mutation round — `VACUUM` (the fixture had no freed pages, so an in-place UPDATE lost the name either way), the `\s+` join (the test erased through `_replaced`'s exact-match branch, which never consults the pattern), the token's length and salt (compared against the wrong slice), and the install disclosure's two print sites (never executed by any test). Each is now asserted against the property rather than the outcome that happened to follow |
| C101 | `pseudonymise_person` reported success while the name was still in the database | 11.7 review — `_scan` folds to NFC and matched; `_swap` built its pattern from the folded name and ran it against the RAW stored bytes, so a macOS-decomposed "José" was found and not replaced. C89's own bug class, fixed for finding and not for replacing. Two defects compounding: the haystack is now normalised too, AND an erasure that leaves any row it found unchanged rolls back and refuses, because there was no partial-failure state at all — the observable result of a total failure was `status: pseudonymised` and a paragraph telling a data subject their name is gone |
| C102 | Two more states where the campaign and the append-only record disagreed about one opinion | 11.5 review — a value of `"Liked"` (the likelier spelling for a model writing prose) was stored and dropped as not a reaction axis, which then costs §11.5's `contested_precedent` finding; and the tag dedupe keyed on VALUE alone, so a second person recording `liked` replaced the first — the §11.5 loss arriving through de-duplication instead of the replace. Axis values are canonicalised at the door, and the dedupe key includes who |
| C103 | C95 was closed for calls landing inside the same second | 11.5 review — the dedupe compared `said_at` against a stamp taken from the clock, so a re-send one second later read as the same person revising their own view. Tags REPLACE, so any second edit that touches them re-sends them. A stated time still makes a second occasion; without one, "again" is the same view |
| C104 | The product-name check refused real people | 11.2 review — a substring scan, so "Themba Nkosi" was the library because of "them", along with Claudette, Automne, Sautoy, Lautoka, Matthey, Serverin and Autolycus. Not a safe failure: a hard block on recording somebody's view, falling hardest on names that are not Anglo, with a remedy ("give another spelling") that does not exist for a person's own name. Now a strike-out: a name is what is left when the words that name nobody are gone, which keeps "Claude Monet" a person and "Claude" not one |
| C105 | The controller statement claimed a privacy the shipped `serve` mode does not have | 11.7 review — "a database only you can read" is false over HTTP, where `auth`'s default provider is `none` and every request is anonymous. It reads the deployment now, the way `_where_it_is` was already made to; it also says "anyone with an account on this machine" in the local case, because a shared machine has other accounts. And it is IN the install disclosure, which C98 called the strongest fact this product has while leaving it reachable only behind a tool call |
| C106 | The one-rule test could only catch a DUPLICATE check, never a missing one | 11.2 review — which is how `normalize_tags` (C99) got in: a door with no check leaves no marker to grep for. `test_every_write_that_takes_a_name_reaches_the_one_guard` walks the AST for every function taking a name-shaped parameter and asserts it reaches `identity.person`. `store.record_authorship` deliberately does NOT count as a guard: it fires after the row is written, which is the half-written record `drift.classify` produced |
| C107 | `health_check` had no `rulebook` component, so an install broken in the one way §12.1 cares about passed the installers' own gate (was D27) | 12.1 — the rulebook is a shipped payload like the CLIP weights and fails the same way: the server answers, the database is fine, and every judgment is missing its rules. It also reports `degraded` when the file in force is the one packaged inside the bundle rather than the one beside the executable — a silent fallback would leave an administrator editing a file nothing reads |
| C108 | `gaps()` could not report a named expected input that no brief supplies (was D50) | 12.1 — the rulebook's `expects` declares them, and the product declares NONE: which inputs a brief must carry is the customer's rule, and inventing it would be the product imposing a requirement on their behalf. `looks_like` is required, because a gap that cannot be closed is one everybody learns to ignore |
| C109 | A competitor's deck filed as `reference` was treated as the customer's rulebook (was D74) | 12.1 — `has_rulebook` meant "any reference record exists", so filing a teardown told the customer their rules were in force and ticked the rulebook step of `readiness`' own shortest path. The rulebook is a declared artefact now, the flag is called `has_reference_material`, and the step says "as reference material" and explains what that buys |
| C110 | Guardrail keyword hits, the sixth computed fact §7.1 asks for (was D92) | 12.1 — a rule may declare `watch_for`, and the server READS the brief for those words and attaches the sentence. With none declared it reports `nothing_to_check`, never `checked_clean`: telling somebody their brief breaches no guardrails when none were looked for is the false pass §11.7 spent a whole item removing from the language checks |
| C111 | The rulebook's first draft shipped six rules that already existed elsewhere | 12.1 review — four were in `EVALUATION_PROCEDURE` word for word (which reaches the model three ways) and two were enforced by validators that refuse the write, so the file whose purpose is being the single versioned home of a rule shipped as a second copy of six. It ships EMPTY: the product owns the judgment discipline, the customer owns the rules about their briefs |
| C112 | Every frozen install would have failed to start | 12.1 review — PyInstaller 6 puts every `datas` entry under `_internal/` whatever destination the spec names, while `config.app_dir()` deliberately resolves beside the executable, so the file the spec shipped and the file the code read were different paths. The build scripts place the visible copy; the bundled one is a fallback that `health_check` reports as degraded |
| C113 | A finding could not cite a rulebook rule, which is the one thing §12.1 claims | 12.1 review — `_verify_quote` resolved every `rule_id` through the CAMPAIGNS table and demanded `record_type == 'reference'`, so the product handed the model six rule ids and refused all of them, while accepting any reference row as a rule. A judgment could carry a breach of a rule its own stamped rulebook did not contain. `rule_id` resolves against the rulebook, the quote is verified against the rule's wording with the same matcher records use, and the finding records which rulebook the rule came from |
| C114 | The startup load was on one entry point of three | 12.1 review — `main.py stdio` had it; `serve` and `stdio_server.py` (what `configure-desktop` wires for a source checkout) did not, so a broken rulebook surfaced as a tool error mid-request and the first tool to crash was `readiness`, whose job is saying what the product cannot do. `stdio_server` also gained a `main()`: it was a bare `__main__` block, which is why nothing noticed when it and `main.py stdio` diverged once already |
| C115 | `readiness` advertised rule-checking on an install with no rules written | 12.1 review — the mechanism shipping is not the capability existing, and a fresh install said "I can say this breaks your own rule" while holding none. It is a `cannot` until rules are written, and what it needs is now a gesture rather than a roadmap number |
| C116 | A customer had nowhere to put their own rules that an upgrade would not overwrite | 12.2 — the overlay lives in the DATA directory beside the database, which no installer touches; the product's own file stays in the install directory. Rules ADD and a rule with the product's id REPLACES it, so writing one rule cannot cost you every shipped one and a customer can still disagree with the product |
| C117 | Two judgments under two different sets of the customer's rules both stamped `core-1.0` | 12.2 — `compare_provenance` diffs `rulebook_version` and concludes two judgments were made "under the same conditions, so an agreement between them is evidence rather than luck", which was a false statement in the tool whose purpose is explaining disagreement. The stamp is `core-1.0+acme-3`: one scalar so every reader stays correct, with the parts readable so nobody has to split it |
| C118 | A customer could not use their own stage names (was D36) | 12.2 — declared statuses are SYNONYMS for the product's three, merged into the table `enums.normalise` already took as a parameter. What they may not declare is `tag_sources`, `metric_types` or `record_types`: those are this library's claims about EVIDENCE, and mapping "confirmed" onto `verified` would make a stated impression outweigh a measured result in every comparison this product makes. Refused by name, with the reason |
| C119 | `region` fed the market grouping, so one campaign counted as two markets (was D71) | 12.2 — the row asked a question and the answer is that a region is a GROUPING of markets. A campaign in Peru whose region is LATAM satisfied §8.3's "seen in at least two markets" gate on its own — a gate whose whole purpose is that breadth has to be earned. Only where the rulebook DECLARES the region: undeclared, `region` is free text meaning a continent on one record and a country on the next, which is why it was a question |
| C120 | `latam` and "Latin America" could never be one market (was D72) (was D73) | 12.2 — declared spellings, resolved where two values are COMPARED rather than written to the row. The first version canonicalised on write and review showed that made a declaration actively harmful: rows written before it kept the old spelling, rows after got the new one, and a query in either found half of them — two spellings that had at least folded to one cell became two. Resolved on comparison, a declaration reaches every row ever written and nothing is rewritten |
| C121 | The 360 channel checklist was the product's, and the scorecard was nobody's (was D93) (was D101) | 12.2 — a declared channel list is fixed per library and per rulebook version, which keeps the property "fixed is the point" was protecting while letting an agency whose work is retail media and CTV stop being told it is missing cinema; `checklist_source` says whose list was used. The scorecard reaches the model the way the rules do, and where none is declared the contract says so and names the file it would go in |
| C122 | Six ways an overlay could silently not apply | 12.2 review — a multi-word spelling (two folds for one lookup, inside the fix for that defect); a status canonical outside the product's three, accepted at load and refusing every write while citing a word the caller never sent; one spelling given to two canonicals, resolving differently in two places; a duplicate YAML key, which PyYAML drops in silence; a dangling symlink read as "no rules"; and a `vocabulary`/`scorecard` section that did not layer at all, so declaring `tags` erased the product's `statuses` |
| C123 | A declared tag could move an opinion off its axis, or invert it | 12.2 review — the seven reaction axes decide the `axis` column on the append-only record and drive every quadrant query, and `tags` could remap them: `not_liked: ['liked']` made a liked campaign read as disliked. That is the reasoning the epistemic guard gives for `tag_sources`, word for word. Spellings that are themselves axis words are refused; a canonical that is one stays allowed, because `not_liked: ['went down badly']` is the point of D73 |
| C124 | Every in-product pointer named the file the product says not to edit | 12.2 review — `readiness` said "write your rules into rulebook.yaml" with no directory, `health_check` printed the install path even when the rules came from the overlay, and its remedy for an unreadable one said to re-run the installer, which cannot touch the data directory. It also now reports an overlay that is ON DISK and not in force, because the rules are read once per process and the customer is invited to write that file under a connector that stays up for days |
| C125 | Eighteen tests read the developer's own data directory | 12.2 review — `overlay_path()` is `DATA_DIR/rulebook.yaml`, and tests that load the rulebook without the `conn` fixture were reading `~/campaign-poc-data`. Empty today, so they passed; the moment anybody did what §12.2 tells them to do, those tests would fail against a file that has nothing to do with them and blame the product. An autouse fixture makes it impossible |
| C126 | `--purge` deleted the customer's rulebook without naming it, and on Linux without asking at all (was D28) | 12.2 — the overlay lives in `DATA_DIR`, which is what both Unix uninstallers offer to delete. macOS printed what was going and required a typed DELETE; Linux did `rm -rf` on a bare flag, which is the trade-off that script's own comment says nobody agreed to. Both now name the rulebook separately, because it is the one file in there the customer WROTE — a library of campaigns can be re-uploaded from the decks and a set of agreed rules cannot |
| C127 | Every remedy said "ask whoever installed this", which the reader usually cannot act on (was D23) | 12.2 — the overlay declares `support:`, and the phrase gains a name wherever it appears. Appended rather than substituted: the rest of the remedy is the part that says what to ask for |
| C128 | The replay was blind to the rulebook itself (was D115) | 12.2 — it is built from `confirmed_at` on registry and correction rows, and a rulebook's rules carry none, so a v1→v2 change produced a replay reporting that nothing had changed: a silent false negative on the axis §8.7 exists for. It could not be built until there were versions to compare, which is what §12.2's stamp made true |
| C129 | "in market" was one agency's phrasing baked into the product's own tables (was D37) | 12.3 — moved to `docs/example-rulebook.yaml`, where an agency that says it finds it in the file they are copying from. Review caught the sweep being half done: `enums` lost it and `mcp_server`'s model-facing gloss still taught it — one decision implemented twice, disagreeing, with the copy a model reads first holding the removed word in place |
| C130 | Standing corrections declared in a customer's rulebook could never reach `expected` (was D108) | 12.3 — they arrive with provenance and no `campaign_id`, so the counting gate refused them forever: seen in 0 campaigns, needs 3. That gate is for a rule the library INFERRED, where breadth across markets is what makes the guess safe; a rule the customer wrote down is not a guess. It routes through `corrections.graduate` with `from_rulebook`, so there is still ONE route to `expected` and one place the conditions live — the first version built the bypass twice, once in the gate (dead) and once around it (live), in the module whose docstring says a second mechanism shaped like the first does not satisfy "one mechanism" |
| C131 | Nine of the ten declared corrections reached no judgment anywhere while the tool reported them in force | 12.3 review — `expected_in = []` already meant "no checklist", so an empty market list could not carry "everywhere", which is what both the loader and the example file said it meant. D108's own failure, shipped as the fix for D108. A column says it now, because the two facts are different: a learned rule is expected where the evidence put it, and only somebody who wrote the rule down can say everywhere. The test that should have caught it asserted a dict that is always truthy. **The column then had to reach the second reader, and for three rounds it did not**: `replay` decided the same question from `expected_in` alone, so the moment a house rule was loaded every saved judgment vanished from the report the graduation had just offered — the tool that puts a rule in force and the tool that says which judgments it reaches, disagreeing about the same rule, which is D114's shape inside the fix for D108. One predicate now (`learning.in_force`), which live evaluation, the replay and the metric registry all call — and the two surfaces that were still answering the same question from the market list alone were found by review in the same round: the preview somebody reads BEFORE confirming a house rule ("every brief in no market … 0 saved judgments", about a rule that reaches all of them) and the gate's own sentence to the model ("already expected of briefs in no market"). The upgrade path needed two hops as well: the first repair wrote `markets or on_file`, leaving a rule that had moved from Peru-only to everywhere applying in every market and still reporting `seen_in: ["Peru"]`, and the guard that was supposed to notice read that row as already reconciled. Sweeping the loader for the same shape found a worse one: a customer who set ONE declared rule aside — through the tool built for that — could never load their rulebook again, because promoting it raised out of the gate and aborted the whole load, so every rule after it went unreconciled too. Reported now instead, which is what the file already does in the other direction: a deleted line does not withdraw a standing rule, and a person's set-aside is not reversed by a file. Two more from the same sweep: two rulebook lines that somebody had answered `same_rule` about resolve to ONE row, which the loader then reconciled back and forth on every start, and a rulebook whose `corrections:` had been emptied returned before the drift sweep ran at all — so deleting one rule of ten was reported and deleting all ten was not. Review found three more readers of the same question: `graduate` made the contradictory row itself on a FRESH install whenever the library had heard the rule from a deck first; `if_graduated`, which is the sentence somebody reads AT the moment they confirm, said a house rule would affect "no market" and 0 judgments; and `standing_for` returned `expected_in` under the name `seen_in`, so the customer was told a rule they had watched the library learn from their own decks was seen nowhere |
| C132 | The example was never shipped, and nothing pointed at it | 12.3 review — not in the PyInstaller spec, not copied by either build script, not named by `init`, the README or the rulebook the product does ship, whose header said §12.3 "will ship" one. A customer installing the built artefact had no example while the product's own comments told them to copy from it |
| C133 | The model was told every standing correction was learned rather than written | 12.3 review — the sentence in front of every judgment said they are "learned from their own feedback, not written by them", which is false twice over for a declared rule. It says which kind each is now, and a declared rule that the library had already heard once provisionally is promoted rather than counted as "already on file" and left provisional forever |
| C134 | Three fields the review named had nowhere to live: where the work actually is, what kind of campaign it is, and who ran it (was D102) (was D103) | 12.4 — `asset_link` is the Figma board or Drive folder somebody opens six months later, which `asset_path` (a file this product copied) is not. `campaign_type` is D102's: the checklist is now KEYED on it where the evidence was type-coherent, so a measure learned entirely from store launches is asked of store launches in every market and of no seeding brief anywhere — market was standing in for the word "launch", and got both halves wrong. `partner` is D103's, and §8.3's gate is named for it — "across at least two PARTNERS or markets" — so `learning.gate` now counts partners through `store.breadth_of`: two agencies writing the same note in one market is real breadth, and it was refused. A column with no reader would have closed the rows and left both defects exactly where they were. `asset_link` is the one of the three with no machine reader and that is deliberate: it is a link for a PERSON to open, which is the whole of what it is for, and inventing a consumer would be worse than saying so |
| C135 | A campaign that was cancelled or paused was REFUSED, with an error explaining there was nowhere to put it (was D38) | 12.4 — and "we stopped this one" is an outcome a library about what works needs most. `on_hold` is a synonym for `paused` rather than a fifth state: two states nothing distinguishes are two checklists for one situation. A cancelled campaign is kept and searchable and is NOT asked for results it will never have. The wire carries all five and a test pins `mcp_server.Status` to `store.VALID_STATUSES` — it published three while the core accepted five, which is §11.5's stripped `said_by` running the other way: a model that believes the schema files a cancelled campaign as `concluded`, and `concluded` is what every outcome gap and calibration figure counts from. The readers were swept with it: `coverage` said `not_yet_run` of a campaign that will never run, and `enums._EXPLAIN` still advised leaving the status unset, which reads as `concluded` |
| C136 | No tool attached a deck to a record that already existed (was D39) (was D51) | 12.4 — so a record whose comments were never read could only gain them by being uploaded again as a duplicate, which §5.2 refused in writing. Found by 5.2's own test that every offered action must name arguments its tool accepts: the obvious offer could not be made. `attach_deck` builds it, reusing the index rebuild rather than copying it, and refuses a record that already has a deck — a new version is a supersession, which keeps both. D51's gap is reported now that it has an action that works, and can be set aside by a record that never had a deck at all. It reads the deck through the SAME paths an upload does — body, commentary, §9.3 promises and the images inside it — because a record repaired the way the gap recommends must not come out thinner than one uploaded whole, and a file nothing can be read from is refused rather than written, so the record stays repairable |
| C137 | A model that guessed an asset's phase produced an unrepairable record (was D123) | 12.4 — `phase` decides whether an image is briefed creative or what actually RAN, and §9.1 says everything falls out of that. `update_asset` takes a reason and a person, because it changes what past evidence meant. Offered where the mistake becomes visible: a comparison with nothing on one side is what a batch filed as `delivered` looks like. `why` is STORED, on `authorship.note`: it was required, echoed back and dropped, so the sentence the tool calls "the part a reader needs six months later" was gone the moment the call returned |
| C138 | A measure that cycled in and out had no record of having done so (was D106) | 12.4 — one `retired_at` cannot say it twice, and "we used to track this" is the point of demoting rather than deleting. No `said_by`: this library retires a measure nobody reports and revives one somebody measures, so there is no person in the loop and naming one would be the product signing its own decision. `retire_stale` passes the observation it computes rather than the generic default, `retire_metric` is guarded on its rowcount like `revive_metric` so the history cannot assert an event that did not happen, and `measure_status` is where it is read — a history nobody can reach is the stored-and-never-applied defect one table along |
| C139 | Whether tracked client comments ARE the `approval_notes` gap (was D15) (was D62) | 12.4 — **they are**, and the item asked for that decision explicitly. A returned deck's comments are what the client wrote when they sent it back, and they arrive with an author, an anchor to the slide and a date, which no retyped free-text box could carry; a second `approval_notes` column would be a parallel store for the same thing, worse in every way. What was genuinely missing is the VERDICT — whether it came back signed off — which is `approval`, `approval_note` and `approval_by` (a PERSON, §11.2). D62 follows: `diff_campaigns` reports what the CLIENT asked for from the earlier deck's comments, beside `adopted` and never merged into it, because one is what this product decided and the other is what the client instructed. It reads the KIND: a `comment` and a PDF `annotation` are somebody writing on the deck, a `speaker_note` is the deck author's own presenter script, and read through a list with the kind stripped off "Presenter: remember to smile" came back as what the client wrote when they sent it back — a false attribution by construction, in the one product whose thesis is that it can say where a claim came from. The author and the anchor travel with each ask. The verdict is REQUIRED to carry a name — validating `said_by` when it happens to arrive is not the same rule as requiring it, and the gap between the two is a row saying `approval: approved` with nobody against it — and it is read beside D62's asks, where not having it changes what a quotation means: "drop the third colourway" under `rejected` is why the deck did not proceed, and under `approved` it is a remark made in passing |
| C140 | A diff could not say what the two decks actually say differently (was D63) | 12.4 — added and removed passages, by layer. From `deck_text`'s own paragraphs rather than the chunks: a chunk is not a passage, because `chunking.pack` merges units at 1800 characters and the first one carries the title, so every version differed on its own title and the whole deck read as replaced. The layer is load-bearing — a paragraph gone from the body is the brief changing, a comment gone is the client's remark being dropped. Commentary is filled first when the reply is trimmed — body first, a fifteen-paragraph rewrite pushed out every dropped client comment, so the cap removed the layer the function calls the more interesting of the two — and passages are folded to NFC, because a deck round-tripped through a Mac reported every accented paragraph as replaced |
| C141 | "Correction" meant three unrelated things (was D113) | 12.4 — a diff's corrections taken, a recomputed metric value, and a standing correction, which carry completely different weight: one is an observation, one is a data fix, one is a rule that judges every future brief in a market. Renaming the stored vocabulary would break every saved row and every tool argument, so each of the THREE surfaces says which it means: `corrections._public`, `core.diff_campaigns` and `metrics.record` on a `recomputed` value |
| C77 | Weighting a client's comment above the deck author's own note (was D10) | 11.5 — and the answer is that it does NOT weigh them. `disagreement` has no `winner` field: authority order is configured in the rulebook (§12) and nothing is configured yet, so preferring the later view, the client's, or the grander job title would be authority nobody granted. §2.5's reason holds — a PDF export turns speaker notes into annotations, so even the FORMAT cannot say whose words weigh more |
| C78 | `author: unknown` convention (was D13) | 11.6 — and a null turned out to be saying FOUR things: `format_carries_none` (a speaker note has no author field), `file_did_not_say` (a PDF annotation can carry `/T` and this one does not), `not_captured` (stored before this library read commentary — a fact about US) and `never_claimed` (body text is not a comment). Told `null`, a judgment citing “a reviewer objected” cannot tell a client's objection from the deck talking to itself |
| C79 | Per-person view and erasure for `campaign_chunks.source.author` (was D14) | 11.7 — `person_on_file` and `pseudonymise_person`. Anonymisation rather than deletion, because deleting leaves a judgment citing nothing and a blank makes “two reviewers objected” indistinguishable from “one reviewer objected twice”. A stable irreversible pseudonym keeps the structure while the name goes |
| C80 | `scope: machine` says “ask whoever installed this” when the current user IS the administrator (was D21) | 11.1/11.4 — `identity.captured_by()` says which account is running the server, so the sentence can tell the two cases apart instead of assuming |
| C81 | `detail` interpolates exception text and asset paths embedding a Windows home directory (was D22) | 11.7 — `people.redact_paths`, applied in `notices.notice` so it reaches every warning rather than the ones somebody remembered. The filename survives because that is what makes a support message diagnosable; the login does not. A filesystem path does not look like personal data until you notice that on two of three platforms it carries somebody's name, in the field this product explicitly says to send to support |
| C82 | The personal-data disclosure belongs at install, and no disclosure text existed (was D26) | 11.7 — `people.install_disclosure()`, GENERATED from `retention()` rather than written beside it. A disclosure maintained separately is the copy that drifts, and here the drift is between what a customer agreed to and what the product does |
| C83 | `.bak` of `claude_desktop_config.json` is a second copy of other connectors' environment, with no retention position (was D30) | 11.7 — named in the install disclosure, including that it may hold other connectors' credentials and is rewritten on every run. It is not this product's data, which is exactly why it needed saying |
| C84 | Author `verified`/`stated` needs its own synonym table (was D35) | 11.2 — `identity.AUTHOR_SOURCES` is exactly `verified` and `stated`, and `TAG_SOURCE_SYNONYMS` is not reused: “measured” is a sentence about a number and meaningless about a person, and a synonym table accepting it would let `source: measured` through on an identity claim |
| C85 | The fact checks are English-only and report `absent` where they mean `unchecked` (was D94) | 11.7 — `facts.language_of`, and this was the product's own central failure in the one place nobody had looked. A Spanish deck's `Presupuesto: 45.000 EUR` is a budget and the library told marketers their decks had none, with the server's authority attached. The checks now report `unchecked`, which this product already distinguishes from a pass. Two calls: when the language cannot be told the checks RUN (a false `unchecked` hides a real finding), and detection is on STOPWORDS, because a marketing deck borrows its content words from English |
| C86 | `confirmed_by` is a free string; nothing distinguishes a real confirmation from one the model wrote itself (was D105) | 11.3 — and the sweep found FIVE implementations of “is this a person”, two checking only for an empty string: `withdraw_context_event(withdrawn_by="the system")` took an event off the record and `link_evaluation(linked_by="Claude")` decided what a judgment was a judgment OF. `learning.require_a_person` — D105's own path, which promotes a rule to the checklist — was one of them. All five now route through `identity.person` |
| C87 | D14's scope has grown: `correction_sightings.provenance` and saved evaluation findings are outside cascade deletion (was D111) | 11.7 — both reached, and the swap inside provenance is WHOLE-WORD rather than wholesale. Replacing “R. Vega, client email, 4 March” entirely would remove when and where a client said something — weakening a standing rule's evidence to remove a name, which this row says explicitly must not happen silently |
| C75 | The override log was write-only: §10.2 gave a person's answer somewhere to go and nothing to read it back with | 10.2 review — `answers`, every statement newest first with `stands`, `silences` and `stale`. For a product whose pitch is that its judgments can be ARGUED WITH, the record of where somebody argued and won is the most valuable thing it holds, and nobody could ask "what have we marked deliberate" or "who set aside which gaps". D116's shape one level down: nobody reviewed the overrides, so a wrong one was permanent in practice though `open` makes it reversible in principle |
| C76 | Nothing was ever re-asked — the queue's founding insight was not applied to the ANSWERS | 10.2 review — `stale_answers_offer`. A gap written off in March is still written off in November and nothing re-asks; no scheduler is invented (this product has none and says so), the age simply stops going unsaid. Gated on `stale` AND `silences`, so it fires on answers that made the library report less and never on a tidy log |
| C62 | `next_actions` carry no staleness token (was D42) | 10.5 — the two places it actually bit are closed rather than a token added to every offer. `menu_token` is 10.5's own and covers the menu; `preview_id` (D117) covers the workbook, which is the only other offer whose argument is large enough that resending it is the problem. A general token on `gaps` or `coverage` would guard a re-read, where a stale answer costs a re-read |
| C63 | Gap ranking by judgments affected rather than a fixed rank per kind — one unindexed record outranks forty (was D52) | 10.2 — by RECORDS affected, not judgments: a library needs a lot of saved judgments before that number means anything, so a ranking resting on it is wrong for every new customer on the day it matters most. Kind stays the primary key and size overrides exactly one step of it (`_MAGNITUDE_SHARE`); `affects`, `share` and `outweighs_its_kind` are in the output, because a number that decides the order and does not appear is one nobody can argue with |
| C64 | An "acknowledged" or "not applicable" state for a gap the user cannot close (was D53) | 10.2 — `answer_gap`. Two states, not one: `acknowledged` stays ranked because it is still the thing to fix, `not_applicable` leaves the ranking because a gap that will never close is a complaint. Silenced in the ranking, never deleted — it stays under `set_aside`, since hiding it would make the product overstate its own coverage |
| C65 | `gaps()` offered at session start and after an upload that changes the top gap (was D54) | 10.6 — both halves. Session start via `gaps_offer`, gated off an empty library where `shortest_path` is already the answer. The upload half needed the previous answer, so `library_state` remembers the top gap and `_gap_moved` fires once per change rather than on every upload; `top_gap` skips `_fields_never_recorded` so the check costs no per-record sweep |
| C66 | A human-answerable path for `no_longer_raised` — recording "yes, we fixed that" after the judgment was saved (was D59) | 10.2 — `answer_finding` with `answer='fixed'`. Kept distinct from `deliberate` (D84) because "we changed it" and "we kept it, and here is why" are the distinction the whole correction loop reasons from |
| C67 | `coverage()` offered from `list_campaigns` and at session start (was D67) | 10.6 — `coverage_offer`, consuming `thin[0]` rather than growing a third grouping, exactly as the row asked. Gated on `thin` being non-empty, which excludes the wholly unmeasured library: `coverage` reports `thin: []` there on purpose and `gaps` answers it better |
| C68 | Three tools answer "what is missing" and should be two (was D76) | 10.6 — `_axis_facts` and `first_steps` are now the single source, read by both `readiness` and `gaps`. `library_is_empty` carries the path's FIRST step instead of `to_first_upload()`'s unordered copy of two of its three, and `gaps()` returns `shortest_path` so the two surfaces cannot disagree about the same library |
| C69 | A departure's rationale has nowhere to live (was D84) | 10.2 — `answer_finding` with `answer='deliberate'`, attached at `store.get_evaluation`, the one place a judgment's full findings load. The stored `departure` is REWRITTEN to `explained`: it is a fact every later judgment reads to decide severity, and after somebody explains the departure `unexplained` is false. Offered from `save_evaluation` whenever a finding asks the question |
| C70 | A partial re-index reports `reindexed.embedded` and nothing else (was D98) | 10.6 — a `chunk_not_embedded` notice, persisted through `_persisted` so it outlives the response, offered `finish_indexing`, and cleared by `_settle_indexing_notice` from every path that changes how much of a record is indexed. It was retracted only by `finish_indexing` at first, so a partial edit followed by a successful one left the record wholly searchable with the notice still open — and the queue then listed it under `needs_attention`, the top-ranked reason a campaign waits on a person. `update_campaign`'s description now says to read it |
| C71 | Deciding two rules are the same is a person's answer, and the question was asked only on the write that raised it (was D110) | 10.2 — `feedback.RULES_ROW`, a fixed row 12 leading to a scope of its own. It was merged into the campaign list first and review rejected that on four counts: the menu's header asks which CAMPAIGN and this row has none; it inverted the column order for one row in a numbered list; it outranked every real campaign, so a session opened on correction wording; and it consumed one of the nine slots §10.2 fixes for campaigns. One row per PAIR, both wordings in full (they matched BECAUSE their openings are alike, so a truncated pair showed two identical strings), dropping out once EITHER side is `asked` — checking only one side asked the same question straight back from the other wording |
| C72 | Every new model-facing tool must be named in a `next_actions` from the write that makes its output non-empty (was D116) | 10.6 — `tests/test_every_tool_is_offered.py`, which is the checklist executed rather than remembered. It found nine unoffered tools on its first run and caught `answer_finding`/`answer_gap` the moment they were added. `save_evaluation`, `correction_status`, `reopen_correction`, `attribute_outcome` and `bulk_import_metrics` are now offered; the rest are exempt with the reason written next to each |
| C73 | A workbook is resent in full to confirm it (was D117) | 10.5 — `import_batches` stages the previewed rows and the preview returns a `preview_id`, so confirming quotes a key and accepting really is one step (no `needs`). Pruned to `MAX_STAGED_IMPORTS`, finished batches first and never an unfinished one |
| C74 | An import stopped by the time budget has no resume key (was D118) | 10.5 — the same key. `imported_through` is written in the same transaction as the rows it counts, so resuming re-imports nothing and re-confirming a finished batch answers `already_imported`. The old note promised "nothing already imported is duplicated" and nothing enforced it — the obvious reading, resending the file, doubled the prefix |
| C52 | `compare_execution` reuses `diff_campaigns`' result vocabulary — `basis`, `counts`, per-item `match`, buckets named for the observation (was D60) | 9.2 — `match` says what a classification RESTS on (`fingerprint`, `visual`, `none`), which is the distinction §9.2 is built around: an identity claim and a resemblance are not the same kind of statement, and the field that carries "what changed" has to say which |
| C51 | `bulk_import_metrics` flattened `BadValue` to a string, so the structured retry was on the single-write path and not the batch one (was D47) | 8.8 — and the plan named this as the likeliest place "Target" arrives, which turned out to be the same sentence twice: the retry now carries `field`/`given`/`valid`/`suggestion`, and `target` is a value rather than a refusal |
| C50 | `graduate_measure` stated no blast radius — "this makes 14 LATAM records show as missing it" (was D104) | 8.7 — and on the OFFER that rides the write, not only on the gate. It was on `graduation()` first, which the model reaches through `measure_status`: the tool §8.4 described as one a user would have to know exists, think to call, and name by canonical stem to use. The decision is made from the offer |
| C1 | Structured warning code for "comments could not be read" | 3.1 — `commentary_unreadable` |
| C2 | The `{code, severity, remedy, detail}` contract lived in one tool's docstring | 3.1 review round — moved to the server `instructions`, so it reaches every tool |
| C3 | `prepare_evaluation` said nothing about a half-indexed library | Closed in the carry-over pass — both surfaces share `_incompleteness_warnings` |
| C4 | Folded warnings read as singular (`count` stored, never spoken) | Same pass — `{count_phrase}` filled by the fold |
| C5 | A legacy `.ppt` emitted two warnings for one condition | Same pass |
| C6 | `version.py` and the Windows installer's `AppVersion` could disagree at release | CI fails a tagged build whose tag does not match `version.py` |
| C8 | Every fresh install failed its own gate — `health_check_cli` opens the database read-only by design, and nothing created it | Phase 4 review round: `campaign-intelligence init` is an install step, which also proves the data location is writable by whoever is running the installer |
| C9 | A refused install still left Claude Desktop wired to the product it had just condemned | Same round: the gate runs before the wiring on all three platforms, and the failure message says so |
| C10 | A failed upgrade destroyed the working copy before verifying the new one | Same round: the Unix installers stage and swap only after the self-test passes |
| C12 | `next_step` prose in warnings (was D16) | 5.2 — `next_actions` with `{label, tool, prefilled_args}`; completed in 5.3, which covered `results_may_be_incomplete` (the last one where a single tool sat behind the advice) |
| C13 | Teaching errors carried the retry only inside a sentence (was D31) | 5.2 — `enums.BadValue` carries `field`, `valid` and `suggestion`, and `_catch_value_errors` passes them through the tool boundary |
| C14 | The one upload-time finding `gaps()` needed could not survive the call that produced it (was D17) | 5.3 — `campaigns.commentary_checked` is persisted and backfilled. **Only that one:** `images_unreadable`, `commentary_capped` and the rest are still ephemeral, which D20 covers |
| C15 | `results_may_be_incomplete` carried `finish_indexing` as prose (was D45) | 5.3 — it carries the action, `consent: "do"`, on every surface that raises it including `prepare_evaluation`. Note those actions are nested inside the warning rather than at the top level of the result |
| C16 | `gaps()` grouped markets itself, so it and the coverage view could describe one library differently — and it ignored the `markets` list (was D55) | 5.5 and its review round. The first attempt was **not** a closure: `gaps()` folded case and `coverage()` did not, so one library gave `gaps()` "no barren market" and `coverage()` two cells of the same market with different markers. Both fold now, on market and collection, verified against `filter_campaign_ids` |
| C17 | An offer whose arguments the user must supply had no defined shape (was D43) | 5.6 — `needs` lists them, verified over the protocol by completing all three first-run steps from what the offers say. (The structural test 5.5's review added checks `to_first_upload`, not the readiness path; the path's own test only requires `needs` to be non-empty) |
| C18 | `coverage`'s `thin` listed every weak cell in a library where all of them were weak (was D66) | 5.6 — it collapses to one summary line plus the first-run path when nothing in the library is measured |
| C38 | Gloss every marketer-facing enum with its synonyms, and say in `instructions` that a normalised value should be spoken (was D32) | 7.4 — `Status`, `MetricType`, `RecordType` and `TagSource` each carry a gloss beside the enum, and the instructions say to tell the user when their word was rewritten |
| C48 | An unknown metric key accepted provisionally rather than raising (was D34) | 8.2 — `metrics.record` registers it, records the value, and asks once; `resolve_measure` takes the answer |
| C47 | A target has no structured home, yet "did we hit our number" is the comparison a marketer most wants (was D33) | 8.1 — `metric_type: target` beside `actual`, with `against_target` answering the comparison. Three answers rather than two: `met` is None when there is no result yet, and None again when the measure has no recorded direction, because under budget is not automatically good |
| C45 | Server stamps `basis: computed` on findings it worked out (was D5) | 7.8 — three mechanical facts become findings, carrying the evidence they were read from, and they never change the verdict |
| C46 | Whether text-similarity matching qualifies as `computed` at all (was D61) | 7.8 — a third value, `heuristic`, with a sentence saying it is repeatable but not a fact: a different threshold would give a different answer |
| C43 | Pin the embedding model by digest rather than by name (was D29) | 7.6 — `ollama/nomic-embed-text@sha256:…` when the embedder will say what it loaded, and the bare name when it will not. A tag moves; a stamp that implies a pin it does not have is the wrong-field failure §7.6 avoids elsewhere |
| C44 | `_migrate_schema` added three campaign columns while readers assumed the whole schema (was D89) | 7.6 — the columns to add are derived from `_SCHEMA` itself, so the class is closed rather than the three instances somebody noticed. A NOT NULL column with no default is added nullable: a null `created_at` on an old row says nobody recorded when, which is true, where `DEFAULT 0` would backdate every record to 1970 |
| C39 | Server fills `provenance` — rulebook/model/server versions (was D4) | 7.6 — four computed fields plus `model_id`, which the server cannot observe and records as unknown unless the caller states it |
| C40 | Notices raised during `prepare_evaluation` belong in the verdict stamp (was D18) | 7.6 — `warnings_at_retrieval`, carried on the receipt and stamped onto the judgment |
| C41 | Nothing counted refused writes (was D81) | 7.6 — counted by reason rather than totalled, so a drift in WHICH rule refuses is visible |
| C42 | A reconciliation against a superseding version sat in the same table as one against measured results (was D85) | 7.6 — `basis: results | superseding_version`, prefilled on §6.3's offer |
| C37 | The layer rule repeated in `prepare_evaluation`'s description and the server `instructions` (was D12) | 7.4 — both are the same `EVALUATION_PROCEDURE` constant, so there is one copy of the rule rather than two that agree |
| C33 | `closest_precedent` computed by the server rather than asserted by the model (was D8) | 7.2 — the top of the recorded retrieval window, marked `computed`; a model-supplied one is refused when a receipt is present, and permitted without one because the server cannot compute what it did not retrieve |
| C34 | Whether a quote was inside the evidence window of the call that produced the judgment (was D77) | 7.2 — the receipt makes the window knowable, and a judgment reports `from_the_window` / `outside_the_window`. §6.1 checks the record contains the words; this checks the reasoner was shown the record |
| C35 | `update_campaign` left the index stale, so search matched the old wording (was D80) | 7.2 — content edits re-chunk and re-embed; metadata edits do not |
| C36 | The disconfirming search queried the judgment's own text when the subject was not a record (was D86) | 7.2 — it reads the subject from the retrieval receipt |
| C30 | Computed facts must run on the body layer only (was D11) | 7.1 — `facts.for_campaign` reads `text_on_file(...)["body"]` and never the commentary; a reviewer's warning about a competitor's budget is not the brief's budget |
| C31 | `gaps()` reports missing fields *within* records (was D49) | 7.1 — `field_never_recorded` per market, two records minimum, `not_applicable` excluded so an out-of-home brief cannot hide a roster with no engagement rates |
| C32 | Fact-level "went stale" across versions (was D58) | 7.1 — `fact_changes` on `diff_campaigns`, reporting a status change rather than a value change |
| C28 | Server fills `evidence` — how much precedent a verdict rests on (was D3) | 6.6 — counted from `cited_ids` rather than the evidence package, with a `strength` code and a sentence; the model may no longer write any of it |
| C29 | Citation concentration per cell — `cited_share_top`, `never_cited` (was D65) | 6.6 — a cell of six measured campaigns where every judgment cites the same one has a real depth of one; `None` rather than 1.0 when nothing has been judged |
| C27 | `approve_if` required on a revise, forbidden on an approve; `fix` required on blocking/should_fix (was D2) | 6.5 — plus forbidden on a REJECT, which the review does not say and which is the only part of "testable" a validator can enforce; plus `exit_checklist`, composed server-side from the fixes so it cannot drift from the findings |
| C26 | `supersedes` never validated to exist; a dangling pointer silently inverts a diff (was D46) | 6.3 — validated on both write paths: the upload path had none at all, so `{"", "  ", "camp_nope"}` could enter the set of superseded ids. Self-reference, dangling ids and cycles refused; a blank is no supersession rather than an error |
| C23 | Supersession offered only on evidence the schema holds, with the replaced record named, and a write path for it (was D41) | 6.3 — gated on a finding id linking the two judgments, never on similarity and never merely on a diff having been run; `update_campaign` can now set `supersedes` and clear it with `""`, which is what makes offering it defensible |
| C24 | Surface the prior evaluation's predictions when a superseding record is judged (was D57) | 6.3 — **the moment moved, deliberately**: D57 said "when a superseding record is judged"; it fires when the supersession is DECLARED, at upload or later via `update_campaign`, because that is when somebody is looking at the thing that settles it. 5.4's findings half still fires at judge time, so the two halves of the loop now fire at different moments — which is right, since the findings get settled structurally there and the forecast can only be settled here |
| C25 | A supersession chain longer than one link (was D64) | 6.3 — `diff_campaigns` walks the chain and reports `through`; a correction recorded at any point along it counts as adopted |
| C22 | `kind` required; a guardrail breach must cite a `rule_id`; the two classes need genuinely different wording (was D1) | 6.1 (the `rule_id` half, including that a breach may not use the `campaign_id` slot) and 6.2 (required `kind`, the `departure` axis, `by_class` and a class-aware note on both the write and the read path). **Closed as written, not as a whole idea:** a departure now asks a rationale and the product still cannot take the answer — D84 |
| C20 | `precedent.quote` verified against what was retrieved (was D6) | 6.1 — verified against the record it cites, which is the fabrication half; the window half is re-opened as D77 rather than closed |
| C21 | A verified quote must match the chunk's LAYER, or verification blesses a commentary quote as something the deck said (was D7) | 6.1 — verification is per layer, both directions, with distinct messages |
| C19 | One campaign in three thin cells had the same fix offered three times (was D68) | 5.6 — `unmeasured_campaigns` groups it per campaign, with the markets it spans |
| C11 | The Windows uninstaller never removed the Claude Desktop connector entry, while both Unix ones did | Same round: `unconfigure-desktop` |
| C7 | `text_search_offline` said "start the local text model service", which names no gesture anyone performs | Caught by the tracker's own staleness test on its first run: it was owed to 4.3, and 4.3 had shipped. The remedy now names re-running the installer, which starts the service, installs the model, and self-tests both |
| C53 | `find_similar_images` and the reuse check searched every asset with no `phase` filter, so event photographs sat in corpora built when "asset" meant "creative" (was D121) | 9.5 — every match now carries `phase` and a `what_it_is` line, and both tools take an optional `phase`. Not excluded: a later deck genuinely reusing an executed photograph IS reuse, and "this matches a photograph of the Bogotá activation" is a real answer — it just has to arrive saying which it is, which is what §9.1 says everything falls out of |
| C54 | A workbook's `Month` or `Launch Date` column was skipped as "not a measurement" rather than read as the period it is (was D119) | 9.6 — §9.6 gave campaigns a window, which is the field it belongs in. A workbook is one row per month so the window widens as rows arrive, never over dates a person typed, and the column is reported as read rather than as skipped — a false statement about the import is the same failure as a silent skip, wearing the other face |
| C55 | §9.8's "caveat travels with the metric" should use the notice shape (was D19) | 9.8 — done in the FACT shape rather than the notice shape, deliberately. A notice is a warning that a capability is degraded: it carries `severity`, `scope` and a remedy, and `visual_search_offline` means the machine cannot do something until somebody acts. A confounded outcome is not a degradation — the product worked perfectly and the number is real. What D19 actually asks for is a stable code beside the prose instead of a private string, and that is delivered: `status` is `confounded` | `checked_clean` | `nothing_to_check` | `not_an_outcome`, matching `execution` (§9.5) and `context` (§9.6), the two facts that sit beside it on every evidence row. Wrapping it as a notice would have made every measured result in a busy market read as a system warning |
| C56 | An evaluation could not be linked to a campaign after the fact, so the store-then-reconcile sequence could not complete (was D40) | 9.9 — `link_evaluation`, offered by `upload_campaign` when a judgment about that same title is attached to nothing. Refused once a link exists: re-pointing a judgment would change what it was a judgment OF, and everything citing it with it |
| C57 | `compare_execution` did not walk the supersession chain, so a re-brief left the judgment on one record and the results on another (was D120) | 9.9 — reconciliation now resolves to the record that RAN, stopping at the first version carrying measured results. Before this it reported `nothing_to_check` and offered to record results against a brief that never ran — the exact failure `actions.after_upload` describes in writing. The gap, the offer and the count follow the chain too |
| C58 | Commitments did not follow a supersession chain (was D127) | 9.9 — the milder half of C57 and closed with it: `_delivered_half` reads the record that ran, so the promises checked are the ones the executed version made |
| C59 | Fold persisted `blocked`/`degraded` notices into the feedback queue (was D20) | 10.1 — `campaign_notices` persists every `blocked` or `degraded` warning against its record, and §10.1's queue ranks it ABOVE every missing tag: something broken outranks a library that knows less. A warning lived for exactly one response, so the thing most wanting somebody's attention was the thing the product forgot fastest |
| C60 | A `single_example` cell as a feedback-queue item (was D70) | 10.1 — a market with one campaign in it is a queue row, gated on the record being otherwise complete and the library being past its first few records. Ungated it fired on every row of a young library, where one campaign per market is true by definition — which is `readiness`' advice, not a queue item |
| C61 | Nothing guides a marketer to tag a measured campaign `performed_well`/`underperformed` as `verified` (was D87) | 10.6 — measurements on file beside a `stated` performance claim is a queue row, and choosing it offers the `update_campaign` that marks it verified. §6.4 was reporting "could not be checked" on libraries that held the evidence |
